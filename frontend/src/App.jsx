export default function App() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">Quiz Management Platform</p>
        <h1>FastAPI + React + PostgreSQL starter</h1>
        <p className="lede">
          Day 1 setup is in place. Backend, frontend, and database scaffolding are ready for
          authentication and quiz workflows.
        </p>
      </section>
    </main>
  )
}

